---
title: minBytes
description: >-
  Creates a pipeline validation action that validates the bytes of a string.
source: /actions/minBytes/minBytes.ts
contributors:
  - fabian-hiller
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# minBytes

Creates a pipeline validation action that validates the bytes of a string.

```ts
const Validation = minBytes<TInput, TRequirement>(requirement, message);
```

## Generics

- `TInput` <Property {...properties.TInput} />
- `TRequirement` <Property {...properties.TRequirement} />

## Parameters

- `requirement` <Property {...properties.requirement} />
- `message` <Property {...properties.message} />

### Explanation

With `minBytes` you can validate the bytes of a string. If the input does not match the `requirement`, you can use `message` to customize the error message.

## Returns

- `Validation` <Property {...properties.Validation} />

## Examples

The following examples show how `minBytes` can be used.

### Min bytes schema

Schema to validate a string with a minimum of 64 bytes.

```ts
const MinBytesSchema = string([
  minBytes(64, 'The string must contain at least 64 bytes.'),
]);
```

## Related

The following APIs can be combined with `minBytes`.

### Schemas

<ApiList items={['any', 'special', 'string', 'unknown']} />
