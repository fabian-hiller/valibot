---
title: AnySchema
description: Any schema type.
contributors:
  - fabian-hiller
---

import { Property } from '~/components';

# AnySchema

Any schema type.

## Definition

- `AnySchema` <Property {...properties.BaseSchema} />
  - `type` <Property {...properties.type} />
  - `pipe` <Property {...properties.pipe} />

export const properties = {
  BaseSchema: {
    type: [
      {
        type: 'custom',
        name: 'BaseSchema',
        href: '../BaseSchema/',
        generics: [
          'any',
          {
            type: 'custom',
            name: 'TOutput',
            default: 'any',
          },
        ],
      },
    ],
  },
  type: {
    type: {
      type: 'string',
      value: 'any',
    },
  },
  pipe: {
    type: [
      {
        type: 'custom',
        name: 'Pipe',
        href: '../Pipe/',
        generics: ['any'],
      },
      'undefined',
    ],
  },
};
