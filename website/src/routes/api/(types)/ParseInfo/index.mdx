---
title: ParseInfo
description: Parse info type.
contributors:
  - fabian-hiller
---

import { Property } from '~/components';

# ParseInfo

Parse info type.

## Definition

- `ParseInfo` <Property {...properties.ParseInfo} />

export const properties = {
  ParseInfo: {
    type: {
      type: 'custom',
      name: 'Partial',
      generics: [
        {
          type: 'custom',
          name: 'Pick',
          generics: [
            {
              type: 'custom',
              name: 'Issue',
              href: '../Issue/',
            },
            [
              { type: 'string', value: 'origin' },
              { type: 'string', value: 'abortEarly' },
              { type: 'string', value: 'abortPipeEarly' },
              { type: 'string', value: 'skipPipe' },
            ],
          ],
        },
      ],
    },
  },
};
