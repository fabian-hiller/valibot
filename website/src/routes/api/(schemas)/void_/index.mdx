---
title: void_
description: Creates a void schema.
source: /schemas/void/void.ts
contributors:
  - fabian-hiller
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# void\_

Creates a void schema.

```ts
const Schema = void_(message);
```

## Parameters

- `message` <Property {...properties.message} />

### Explanation

With `void_` you can validate the data type of the input and if it is not `undefined`, you can use `message` to customize the error message.

## Returns

- `Schema` <Property {...properties.Schema} />

## Related

The following APIs can be combined with `void_`.

### Schemas

<ApiList
  items={[
    'array',
    'intersect',
    'map',
    'nonNullable',
    'nonNullish',
    'nonOptional',
    'nullable',
    'nullish',
    'object',
    'optional',
    'record',
    'recursive',
    'set',
    'tuple',
    'union',
  ]}
/>

### Methods

<ApiList
  items={[
    'brand',
    'coerce',
    'fallback',
    'getDefault',
    'getDefaults',
    'getFallback',
    'getFallbacks',
    'is',
    'parse',
    'safeParse',
    'transform',
  ]}
/>
