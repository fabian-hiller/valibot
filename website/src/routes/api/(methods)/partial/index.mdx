---
title: partial
description: >-
  Creates an object schema consisting of all properties of an existing object
  schema set to optional.
source: /methods/partial/partial.ts
contributors:
  - fabian-hiller
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# partial

Creates an object schema consisting of all properties of an existing object schema set to optional.

```ts
// Partial object schema with an optional pipe
const Schema = partial<TSchema>(schema, pipe);

// Partial object schema with an optional message and pipe
const Schema = partial<TSchema>(schema, message, pipe);

// Partial object schema with rest and optional pipe
const Schema = partial<TSchema, TRest>(schema, rest, pipe);

// Partial object schema with rest and optional message and pipe
const Schema = partial<TSchema, TRest>(schema, rest, message, pipe);
```

## Generics

- `TSchema` <Property {...properties.TSchema} />
- `TRest` <Property {...properties.TRest} />

## Parameters

- `schema` <Property {...properties.schema} />
- `rest` <Property {...properties.rest} />
- `message` <Property {...properties.message} />
- `pipe` <Property {...properties.pipe} />

### Explanation

`partial` creates a new object schema where the entries of the provided object `schema` are optional. It is similar to TypeScript's [`Partial`](https://www.typescriptlang.org/docs/handbook/utility-types.html#partialtype) utility type.

## Returns

- `Schema` <Property {...properties.Schema} />

## Examples

The following examples show how `partial` can be used.

### Partial object schema

Schema to validate an object with partial entries.

```ts
const PartialObjectSchema = partial(
  object({
    key1: string(),
    key2: number(),
  })
);
```

## Related

The following APIs can be combined with `partial`.

### Schemas

<ApiList
  items={[
    'any',
    'array',
    'bigint',
    'blob',
    'boolean',
    'date',
    'enum_',
    'instance',
    'intersect',
    'literal',
    'map',
    'nan',
    'never',
    'nonNullable',
    'nonNullish',
    'nonOptional',
    'null_',
    'nullable',
    'nullish',
    'number',
    'object',
    'optional',
    'picklist',
    'record',
    'recursive',
    'set',
    'special',
    'string',
    'symbol',
    'tuple',
    'undefined_',
    'union',
    'unknown',
    'variant',
    'void_',
  ]}
/>

### Methods

<ApiList
  items={[
    'brand',
    'coerce',
    'fallback',
    'forward',
    'getDefault',
    'getDefaults',
    'getFallback',
    'getFallbacks',
    'is',
    'keyof',
    'merge',
    'omit',
    'parse',
    'pick',
    'required',
    'safeParse',
    'transform',
    'unwrap',
  ]}
/>

### Transformations

<ApiList items={['toCustom']} />

### Validations

<ApiList items={['custom']} />
