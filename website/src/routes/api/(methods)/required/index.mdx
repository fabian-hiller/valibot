---
title: required
description: >-
  Creates an object schema consisting of all properties of an existing object
  schema set to non optional.
source: /methods/required/required.ts
contributors:
  - fabian-hiller
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# required

Creates an object schema consisting of all properties of an existing object schema set to non optional.

```ts
// Required object schema with an optional pipe
const Schema = required<TSchema>(schema, pipe);

// Required object schema with an optional message and pipe
const Schema = required<TSchema>(schema, message, pipe);

// Required object schema with rest and optional pipe
const Schema = required<TSchema, TRest>(schema, rest, pipe);

// Required object schema with rest and optional message and pipe
const Schema = required<TSchema, TRest>(schema, rest, message, pipe);
```

## Generics

- `TSchema` <Property {...properties.TSchema} />
- `TRest` <Property {...properties.TRest} />

## Parameters

- `schema` <Property {...properties.schema} />
- `rest` <Property {...properties.rest} />
- `message` <Property {...properties.message} />
- `pipe` <Property {...properties.pipe} />

### Explanation

`required` creates a new object schema where the entries of the provided object `schema` are non optional. It is similar to TypeScript's [`Required`](https://www.typescriptlang.org/docs/handbook/utility-types.html#requiredtype) utility type.

> Note that `rest`, `message` and `pipe` of the passed schema are completely ignored and must be added again.

## Returns

- `Schema` <Property {...properties.Schema} />

## Examples

The following examples show how `required` can be used.

### Required object schema

Schema to validate an object with required entries.

```ts
const RequiredObjectSchema = required(
  object({
    key1: optional(string()),
    key2: optional(number()),
  })
);
```

## Related

The following APIs can be combined with `required`.

### Schemas

<ApiList
  items={[
    'any',
    'array',
    'bigint',
    'blob',
    'boolean',
    'date',
    'enum_',
    'instance',
    'intersect',
    'literal',
    'map',
    'nan',
    'never',
    'nonNullable',
    'nonNullish',
    'nonOptional',
    'null_',
    'nullable',
    'nullish',
    'number',
    'object',
    'optional',
    'picklist',
    'record',
    'recursive',
    'set',
    'special',
    'string',
    'symbol',
    'tuple',
    'undefined_',
    'union',
    'unknown',
    'variant',
    'void_',
  ]}
/>

### Methods

<ApiList
  items={[
    'brand',
    'coerce',
    'fallback',
    'forward',
    'getDefault',
    'getDefaults',
    'getFallback',
    'getFallbacks',
    'is',
    'keyof',
    'merge',
    'omit',
    'parse',
    'partial',
    'pick',
    'safeParse',
    'transform',
    'unwrap',
  ]}
/>

### Transformations

<ApiList items={['toCustom']} />

### Validations

<ApiList items={['custom']} />
