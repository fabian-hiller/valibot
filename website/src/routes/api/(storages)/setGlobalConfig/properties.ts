import type { PropertyProps } from '~/components';

export const properties: Record<string, PropertyProps> = {
  config: {
    type: {
      type: 'custom',
      name: 'GlobalConfig',
      href: '../GlobalConfig/',
    },
  },
};
