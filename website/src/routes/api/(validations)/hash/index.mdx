---
title: hash
source: /validations/hash/hash.ts
contributors:
  - fabian-hiller
  - ariskemper
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# hash

Creates a pipeline validation action that validates a hash.

```ts
const Validation = hash<TInput>(message);
```

## Generics

- `TInput` <Property {...properties.TInput} />

## Parameters

- `types` <Property {...properties.types} />

- `message` <Property {...properties.message} />

### Explanation

With `hash` you can validate the formatting of a string. If the input is not a hash, you can use `message` to customize the error message.
If no types are provided, it validate against all the types, otherwise it will validate against the ones that are provided.

Supported types are: md4, md5, sha1, sha256, sha384, sha512, ripemd128, ripemd160, tiger128, tiger160, tiger192, crc32, crc32b, adler32

## Returns

- `Validation` <Property {...properties.Validation} />

## Examples

The following examples show how `hash` can be used.

### Hash schema

Schema to validate an hash.

```ts
const HashSchema = hash('Hash is invalid.');
```

Schema to validate an hash with array of valid hash params to validate.

```ts
const HashSchema = hash(['md5', 'sha1'], 'Hash is invalid.');
```

## Related

The following APIs can be combined with `hash`.

### Schemas

<ApiList items={['any', 'string', 'unknown']} />
