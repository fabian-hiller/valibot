---
title: uuid
description: Creates a pipeline validation action that validates a UUID.
source: /validations/uuid/uuid.ts
contributors:
  - fabian-hiller
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# uuid

Creates a pipeline validation action that validates a [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier).

```ts
const Validation = uuid<TInput>(message);
```

## Generics

- `TInput` <Property {...properties.TInput} />

## Parameters

- `message` <Property {...properties.message} />

### Explanation

With `uuid` you can validate the formatting of a string. If the input is not an UUID, you can use `message` to customize the error message.

## Returns

- `Validation` <Property {...properties.Validation} />

## Examples

The following examples show how `uuid` can be used.

### UUID schema

Schema to validate an UUID.

```ts
const UuidSchema = string([uuid('The UUID is badly formatted.')]);
```

## Related

The following APIs can be combined with `uuid`.

### Schemas

<ApiList items={['any', 'special', 'string', 'unknown']} />
