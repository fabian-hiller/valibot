---
title: mac48
description: Creates a pipeline validation action that validates a mac 48 bit address.
source: /validations/mac48/mac48.ts
contributors:
  - fabian-hiller
  - ariskemper
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# mac48

Creates a pipeline validation action that validates a mac 48 bit address. It validates 48 bit and 64 bit mac addresses.
If you want to validate both 48 bit mac address and 64 bit address, you can use `mac` validation action and if you want
to validate the 64 bit mac address, you can use `mac64` validation action.

```ts
const Validation = mac48<TInput>(message);
```

## Generics

- `TInput` <Property {...properties.TInput} />

## Parameters

- `message` <Property {...properties.message} />

### Explanation

With `mac48` you can validate the formatting of a string. If the input is not a mac 48 bit address, you can use `message` to customize the error message.

## Returns

- `Validation` <Property {...properties.Validation} />

## Examples

The following examples show how `mac48` can be used.

### Mac 48 bit address schema

Schema to validate a mac 48 bit address.

```ts
const Mac48AddressSchema = string([
  mac48('The mac address is badly formatted.'),
]);
```

## Related

The following APIs can be combined with `mac48`.

### Schemas

<ApiList items={['any', 'string', 'unknown']} />
