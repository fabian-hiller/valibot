---
title: mac48
description: Creates a pipeline validation action that validates a 48-bit MAC address.
source: /validations/mac48/mac48.ts
contributors:
  - fabian-hiller
  - ariskemper
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# mac48

Creates a pipeline validation action that validates a 48-bit [MAC address](https://en.wikipedia.org/wiki/MAC_address).

```ts
const validation = mac48<TInput>(message);
```

## Generics

- `TInput` <Property {...properties.TInput} />

## Parameters

- `message` <Property {...properties.message} />

### Explanation

With `mac48` you can validate the formatting of a string. If the input is not a 48-bit MAC address, you can use `message` to customize the error message.

## Returns

- `validation` <Property {...properties.validation} />

## Examples

The following examples show how `mac48` can be used.

### 48-bit MAC schema

Schema to validate a 48-bit MAC address.

```ts
const Mac48Schema = string([mac48('The MAC address is badly formatted.')]);
```

## Related

The following APIs can be combined with `mac48`.

### Schemas

<ApiList items={['any', 'string', 'unknown']} />
